describe('Unit(plantsService)', () => {
  it('should send the api response correctly if no server error', () => {
    // TODO
  });

  it('should return a ApiError response when catch some error', () => {
    // TODO
  });
});
